      %% List of Nodes on which cover will be active during test.
      %% Nodes = [atom()]
      {nodes, ['dev@Ulfs-IMac-3.local']}.       
      %% Cover analysis level.
      %% Level = details | overview
      {level, details}.       
      %% Directories to include in cover.
      %% Dirs = [string()]
      {incl_dirs, ["/Users/ua/projekte/erlang/lilly/ebin", "/Users/ua/projekte/erlang/lilly/src"]}.
      %% Directories, including subdirectories, to include.
      {incl_dirs_r, ["/Users/ua/projekte/erlang/lilly/include"]}.
